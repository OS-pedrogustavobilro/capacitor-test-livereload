# capacitor-test-livereload

To test different PRs in Capacitor that are fixing or adding capability to live reload functionality

You just need to point the capacitor dependencies (mainly CLI) to the local capacitor project that you clone from the PR containing the changes.
